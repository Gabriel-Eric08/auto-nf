<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visualizar Itens do Contrato</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    
    <style>
        body {
            background-color: #f9f9f9;
        }
        /* ... Estilos não relacionados à impressão (mantidos) ... */
        .navbar-custom {
            background-color: #ffffff;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
            border-bottom: 3px solid #d81b60;
        }
        .navbar-custom .nav-link,
        .navbar-custom .navbar-brand {
            color: #d81b60;
            font-weight: bold;
        }
        .navbar-custom .nav-link:hover {
            color: #ff69b4;
        }
        .dropdown-menu {
            border: none;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
        }
        .dropdown-item {
            color: #d81b60;
            transition: background-color 0.3s ease;
        }
        .dropdown-item:hover {
            background-color: #fff0f5;
        }
        .nav-item.dropdown:hover .dropdown-menu {
            display: block;
        }
        .card-custom {
            border: 1px solid #d81b60;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
        }
        .btn-custom {
            background-color: #d81b60;
            color: white;
            border: none;
        }
        .btn-custom:hover {
            background-color: #ff69b4;
            color: white;
        }
        .btn-acao {
            background-color: #4a148c; /* Cor roxa */
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            display: inline-block;
        }
        .btn-acao:hover {
            background-color: #7b1fa2;
            color: white;
        }
        .btn-salvar {
            background-color: #4a148c;
            color: white;
            border: none;
        }
        .btn-salvar:hover {
            background-color: #7b1fa2;
            color: white;
        }
        .table-custom th, .table-custom td {
            border: 1px solid #d81b60;
            vertical-align: middle;
            /* Diminui o padding horizontal para ganhar espaço */
            padding-left: 0.3rem !important;
            padding-right: 0.3rem !important;
        }
        .table-custom th {
            background-color: #d81b60;
            color: white;
            font-size: 0.8rem; /* Diminui a fonte do cabeçalho */
        }
        .table-custom td {
             font-size: 0.7rem; /* Diminui a fonte do corpo */
        }
        .table-custom tr:nth-child(even) {
            background-color: #fce4ec;
        }
        .table-custom tr:nth-child(odd) {
            background-color: #fff0f5;
        }
        .form-control[readonly] {
            background-color: #e9ecef;
            opacity: 1;
        }

        /* --- ESTILOS DE IMPRESSÃO (CORRIGIDOS) --- */
        @media print {
            .no-print, .navbar-custom {
                display: none !important;
            }
            body {
                background-color: white !important;
                /* Reduz a margem do corpo da página para maximizar a área de impressão */
                margin: 0 !important;
                padding: 0 !important;
            }
            .container {
                /* Remove as margens laterais padrão do container do Bootstrap */
                width: 100% !important;
                max-width: none !important;
                margin: 0 !important;
                padding: 10px 10px 0 10px !important; /* Adiciona um padding pequeno */
            }
            .card-custom {
                border: none !important;
                box-shadow: none !important;
            }
            
            /* Ajustes cruciais para a tabela */
            .table-responsive {
                overflow: visible !important; /* Garante que não haja scroll */
            }
            .table-custom {
                -webkit-print-color-adjust: exact;
                color-adjust: exact;
                width: 100% !important;
                table-layout: fixed; /* Força a tabela a respeitar a largura definida */
            }
            .table-custom th, .table-custom td {
                font-size: 8pt !important; /* Tamanho de fonte bem pequeno */
                padding: 2px 3px !important; /* Reduz o padding interno */
                white-space: normal; /* Permite que o texto quebre linha */
            }
            
            /* Define larguras relativas para as colunas mais estreitas */
            .table-custom th:nth-child(4), /* Quantidade */
            .table-custom th:nth-child(5), /* Valor Unitário */
            .table-custom th:nth-child(6), /* Valor Total */
            .table-custom th:nth-child(7), /* Quantidade 25% */
            .table-custom th:nth-child(8)  /* Valor Total 25% */
            {
                width: 10%; /* Dá 10% de largura para as colunas de valores/quantidade */
            }

            .table-custom th:nth-child(1), /* Código do Produto */
            .table-custom th:nth-child(3) /* Lote */
            {
                 width: 15%; 
            }
             .table-custom th:nth-child(2) /* Descrição */
            {
                 width: 30%; 
            }
            
            .print-header::before {
                content: "Relatório de Itens do Contrato - {% if contrato_selecionado %}{{ contrato_selecionado.nome }}{% endif %}";
                display: block;
                font-size: 1.2rem;
                font-weight: bold;
                margin: 0 0 15px 0;
                padding-bottom: 5px;
                border-bottom: 2px solid #333;
            }
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-custom no-print">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ url_for('home.home') }}">Secretaria da Mulher</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Contratos
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ url_for('contrato.contratos_page') }}">Cadastro contrato</a></li>
                        <li><a class="dropdown-item" href="{{ url_for('contrato_produto.contrato_produto_page') }}">Cadastro nota de empenho</a></li>
                        <li><a class="dropdown-item" href="{{ url_for('contrato_produto.visualizar_contrato_produto_page') }}">Visualizar notas de empenho</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Notas Fiscais
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ url_for('nota_fiscal.cadastro_nota_fiscal_page') }}">Cadastrar nota fiscal</a></li>
                        <li><a class="dropdown-item" href="{{ url_for('nota_fiscal.visualizar_notas_fiscais_page') }}">Visualizar notas fiscais</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Controle
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ url_for('controle_gasto.controle_gasto_page') }}">Controle gastos</a></li>
                    </ul>
                </li>
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <span class="material-icons">
                            account_circle
                        </span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="#">Trocar de conta</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5">
    
    <div class="card card-custom no-print">
        <div class="card-header bg-white">
            <h5 class="card-title" style="color: #d81b60;">Visualizar Itens do Contrato</h5>
        </div>
        <div class="card-body">
            <form action="{{ url_for('contrato_produto.visualizar_contrato_produto_page') }}" method="POST">
                <div class="row g-3 mb-3">
                    <div class="col-md-4">
                        <label for="selectContrato" class="form-label">Contrato</label>
                        <select class="form-select" id="selectContrato" name="contrato_id" required>
                            <option selected disabled value="">Selecione um contrato...</option>
                            {% for contrato in contratos %}
                            <option value="{{ contrato.id }}" data-casa="{{ contrato.casa }}" data-tipo="{{ contrato.tipo }}" {% if contrato_selecionado and contrato.id == contrato_selecionado.id %} selected {% endif %}>{{ contrato.nome }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    
                    <div class="col-md-4">
                        <label for="selectLote" class="form-label">Lote</label>
                        <select class="form-select" id="selectLote" name="lote_id">
                            <option value="">Todos os Lotes</option>
                            {% for lote in lotes %}
                            <option value="{{ lote.id }}" {% if lote_id_enviado and lote.id | string == lote_id_enviado | string %} selected {% endif %}>{{ lote.nome_lote }}</option>
                            {% endfor %}
                        </select>
                    </div>
                    
                    <div class="col-md-4">
                        <label for="casaContrato" class="form-label">Casa</label>
                        <input type="text" class="form-control" id="casaContrato" readonly value="{% if contrato_selecionado %}{{ contrato_selecionado.casa }}{% endif %}">
                    </div>
                    <div class="col-md-4">
                        <label for="tipoContrato" class="form-label">Tipo</label>
                        <input type="text" class="form-control" id="tipoContrato" readonly value="{% if contrato_selecionado %}{{ contrato_selecionado.tipo }}{% endif %}">
                    </div>
                </div>
                <div class="d-grid gap-2 d-md-flex justify-content-md-start mt-3">
                    <button type="submit" class="btn btn-custom" id="btnConsultar">Consultar</button>
                    <button type="button" onclick="window.print()" class="btn-acao">
                        <i class="material-icons me-1" style="vertical-align: middle;">print</i> Imprimir/Salvar PDF
                    </button>
                </div>
            </form>
        </div>
    </div>

    <h3 class="mt-5 print-header" style="color: #d81b60;">Produtos do Contrato</h3>
    
    <div class="table-responsive">
        <table class="table table-striped table-hover table-custom">
            <thead>
                <tr>
                    <th scope="col">Cód. Prod.</th> <th scope="col">Descrição</th> <th scope="col">Lote</th> <th scope="col">Quant.</th> <th scope="col">Vlr Unit.</th> <th scope="col">Vlr Total</th> <th scope="col">Quant. 25%</th> <th scope="col">Vlr Total 25%</th> </tr>
            </thead>
            <tbody id="tabela-produtos">
                {% if produtos_contrato %}
                {% for cp, produto, lote in produtos_contrato %} 
                    <tr>
                        <td>{{ produto.nome }}</td>
                        <td>{{ produto.descricao }}</td>
                        <td>{{ lote.nome_lote }}</td> 
                        <td>{{ cp.quantidade_max }}</td>
                        <td>R$ {{ "%.2f"|format(cp.preco_unitario) }}</td>
                        <td>R$ {{ "%.2f"|format(cp.quantidade_max * cp.preco_unitario) }}</td>
                        <td>{{ (cp.quantidade_max / 4) | int }}</td>
                        <td>R$ {{ "%.2f"|format((cp.quantidade_max * cp.preco_unitario) / 4) }}</td>
                    </tr>
                {% endfor %}
                {% else %}
                <tr>
                    <td colspan="8" class="text-center">Nenhum produto para exibir. Selecione e consulte um contrato e/ou lote.</td>
                </tr>
                {% endif %}
            </tbody>
        </table>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    document.getElementById('selectContrato').addEventListener('change', function() {
        const selectedOption = this.options[this.selectedIndex];
        
        const casa = selectedOption.dataset.casa;
        const tipo = selectedOption.dataset.tipo;

        document.getElementById('casaContrato').value = casa || '';
        document.getElementById('tipoContrato').value = tipo || '';
    });
</script>

</body>
</html>