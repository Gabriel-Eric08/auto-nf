<!DOCTYPE html>
<html lang="pt-br" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes da Nota Fiscal - {{ nf_data.nome_nf }}</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --primary-color: #6f42c1;
            --primary-hover: #59359a;
            --bg-body: #f8f9fa;
            --card-bg: #ffffff;
            --text-color: #333333;
            --border-color: #e0e0e0;
            --transition: all 0.3s ease;
        }

        [data-theme="dark"] {
            --bg-body: #121212;
            --card-bg: #1e1e1e;
            --text-color: #e0e0e0;
            --border-color: #444444;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--bg-body);
            color: var(--text-color);
            transition: var(--transition);
        }

        .container { margin-top: 40px; margin-bottom: 40px; }

        /* Card Moderno */
        .card-custom {
            background: var(--card-bg);
            border: none;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            overflow: hidden;
        }

        .card-header-visual {
            background: linear-gradient(45deg, var(--primary-color), var(--primary-hover));
            padding: 2rem;
            color: white;
        }

        /* Tabela Detalhada */
        .table-modern {
            border-radius: 12px;
            overflow: hidden;
            border: 1px solid var(--border-color);
        }

        .table-modern thead {
            background-color: rgba(111, 66, 193, 0.05);
        }

        .table-modern th {
            border: none;
            color: var(--primary-color);
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 1px;
            padding: 15px;
        }

        .table-modern td {
            padding: 15px;
            border-bottom: 1px solid var(--border-color);
            vertical-align: middle;
        }

        .table-footer-total {
            background-color: var(--primary-color);
            color: white;
            font-weight: 700;
        }

        /* Botões */
        .btn-custom {
            border-radius: 10px;
            padding: 10px 20px;
            font-weight: 500;
            transition: var(--transition);
            border: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-voltar { background: #6c757d; color: white; }
        .btn-voltar:hover { background: #5a6268; transform: translateX(-3px); }

        .btn-print { background: var(--primary-color); color: white; }
        .btn-print:hover { background: var(--primary-hover); transform: translateY(-2px); }

        /* Estilos de Impressão */
        @media print {
            body { background: white !important; color: black !important; }
            .no-print { display: none !important; }
            .container { margin: 0; max-width: 100%; width: 100%; }
            .card-custom { box-shadow: none !important; border: 1px solid #eee !important; }
            .card-header-visual { background: white !important; color: var(--primary-color) !important; padding: 1rem 0; border-bottom: 2px solid var(--primary-color); }
            .table-modern { border: 1px solid #333 !important; }
            .table-modern th { color: black !important; border-bottom: 1px solid #333 !important; }
            .table-footer-total { background-color: #eee !important; color: black !important; border-top: 2px solid black !important; }
        }
    </style>
</head>
<body>

<div class="container">
    <div class="card card-custom">
        <div class="card-header-visual d-flex justify-content-between align-items-center">
            <div>
                <h5 class="mb-1 opacity-75 text-uppercase small fw-bold">Documento de Conferência</h5>
                <h2 class="mb-0 fw-bold">{{ nf_data.nome_nf }}</h2>
            </div>
            <div class="no-print">
                <i class="fa-solid fa-file-invoice fa-3x opacity-50"></i>
            </div>
        </div>

        <div class="card-body p-4 p-md-5">
            <div class="d-flex justify-content-between align-items-center mb-5 no-print">
                <div class="d-flex gap-2">
                    <a href="{{ url_for('home.home') }}" class="btn-custom btn-voltar text-decoration-none">
                        <i class="fa-solid fa-arrow-left"></i> Voltar
                    </a>
                    <button onclick="window.print()" class="btn-custom btn-print">
                        <i class="fa-solid fa-print"></i> Imprimir ou PDF
                    </button>
                </div>
                </div>

            <div class="row g-4 mb-5">
                <div class="col-md-6">
                    <div class="p-3 rounded-4 border border-light shadow-sm bg-light-subtle">
                        <h6 class="text-primary fw-bold mb-3"><i class="fa-solid fa-calendar-day me-2"></i>Dados da Emissão</h6>
                        <p class="mb-1"><strong>Data:</strong> {{ nf_data.data_emissao }}</p>
                        <p class="mb-0"><strong>Status:</strong> <span class="badge bg-success">Recebido</span></p>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="p-3 rounded-4 border border-light shadow-sm bg-light-subtle">
                        <h6 class="text-primary fw-bold mb-3"><i class="fa-solid fa-file-contract me-2"></i>Vínculo Contratual</h6>
                        <p class="mb-1"><strong>Contrato:</strong> {{ nf_data.contrato_nome }}</p>
                        <p class="mb-1"><strong>Casa:</strong> {{ nf_data.contrato_casa }}</p>
                        <p class="mb-0"><strong>Tipo:</strong> {{ nf_data.contrato_tipo }}</p>
                    </div>
                </div>
            </div>

            <hr class="my-5 opacity-25">

            <h5 class="fw-bold mb-4" style="color: var(--primary-color);">
                <i class="fa-solid fa-boxes-stacked me-2"></i>Itens da Nota Fiscal
            </h5>
            
            <div class="table-responsive">
                <table class="table table-modern">
                    <thead>
                        <tr>
                            <th scope="col">Produto</th>
                            <th scope="col">Descrição</th>
                            <th scope="col" class="text-center">Quantidade</th>
                            <th scope="col">Preço Unitário</th>
                            <th scope="col">Subtotal</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for produto in nf_data.produtos %}
                        <tr>
                            <td class="fw-bold">{{ produto.nome_produto }}</td>
                            <td class="text-muted small">{{ produto.descricao_produto }}</td>
                            <td class="text-center">
                                <span class="badge rounded-pill bg-light text-dark border px-3">{{ produto.quantidade_recebida }}</span>
                            </td>
                            <td>R$ {{ "%.2f"|format(produto.preco_unitario_nf) }}</td>
                            <td class="fw-bold text-primary">R$ {{ "%.2f"|format(produto.valor_total) }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                    <tfoot>
                        <tr class="table-footer-total">
                            <td colspan="4" class="text-end py-3 text-uppercase small">Valor Total da Nota</td>
                            <td class="py-3 fs-5">R$ {{ "%.2f"|format(nf_data.total_geral) }}</td>
                        </tr>
                    </tfoot>
                </table>
            </div>

            <div class="mt-5 d-none d-print-block">
                <div class="row text-center mt-5">
                    <div class="col-6">
                        <div style="border-top: 1px solid #000; padding-top: 10px; margin-top: 50px;">
                            Responsável pelo Recebimento
                        </div>
                    </div>
                    <div class="col-6">
                        <div style="border-top: 1px solid #000; padding-top: 10px; margin-top: 50px;">
                            Data e Carimbo
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    // Sincroniza o tema com o localStorage (mesmo script das outras páginas)
    const savedTheme = localStorage.getItem('theme') || 'light';
    document.documentElement.setAttribute('data-theme', savedTheme);
</script>

</body>
</html>