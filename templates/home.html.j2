    <!DOCTYPE html>
    <html lang="pt-br" data-theme="light">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Início - Sec. Mulher PE</title>
        
        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        
        <style>
            :root {
                --primary-color: #6f42c1;
                --primary-hover: #59359a;
                --accent-color: #a673e8;
                --bg-body: #f8f9fa;
                --card-bg: #ffffff;
                --text-color: #333333;
                --nav-bg: rgba(255, 255, 255, 0.85);
                --transition: all 0.3s ease;
            }

            [data-theme="dark"] {
                --bg-body: #121212;
                --card-bg: #1e1e1e;
                --text-color: #e0e0e0;
                --nav-bg: rgba(30, 30, 30, 0.85);
            }

            body {
                font-family: 'Poppins', sans-serif;
                background-color: var(--bg-body);
                color: var(--text-color);
                transition: var(--transition);
                overflow-x: hidden;
            }

            /* Navbar Moderna com Glassmorphism */
            .navbar-custom {
                background: var(--nav-bg);
                backdrop-filter: blur(12px);
                -webkit-backdrop-filter: blur(12px);
                box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
                border-bottom: 1px solid rgba(111, 66, 193, 0.1);
                padding: 0.8rem 0;
            }

            .navbar-brand {
                font-weight: 700;
                color: var(--primary-color) !important;
                font-size: 1.4rem;
            }

            .nav-link {
                color: var(--text-color) !important;
                font-weight: 500;
                transition: var(--transition);
                margin: 0 5px;
            }

            .nav-link:hover {
                color: var(--primary-color) !important;
                transform: translateY(-1px);
            }

            /* Estilo Dropdown */
            .dropdown-menu {
                border: none;
                border-radius: 15px;
                box-shadow: 0 10px 30px rgba(0,0,0,0.1);
                background-color: var(--card-bg);
                padding: 10px;
                margin-top: 10px;
            }

            .dropdown-item {
                border-radius: 8px;
                padding: 10px 20px;
                color: var(--text-color);
                font-weight: 300;
            }

            .dropdown-item:hover {
                background-color: var(--primary-color);
                color: white !important;
            }

            /* Seção Principal */
            .main-hero {
                height: 90vh;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                background: radial-gradient(circle at top right, rgba(111, 66, 193, 0.05), transparent);
            }

            .hero-title {
                font-weight: 700;
                font-size: clamp(2.5rem, 8vw, 5rem);
                color: var(--primary-color);
                margin-bottom: 1rem;
                animation: fadeInUp 0.8s ease;
            }

            .hero-subtitle {
                font-weight: 300;
                font-size: 1.2rem;
                max-width: 600px;
                opacity: 0.8;
                animation: fadeInUp 1s ease;
            }

            /* Toggle Theme Button */
            .theme-switch {
                background: var(--primary-color);
                color: white;
                border: none;
                width: 40px;
                height: 40px;
                border-radius: 12px;
                display: flex;
                align-items: center;
                justify-content: center;
                cursor: pointer;
                transition: var(--transition);
            }

            .theme-switch:hover {
                transform: scale(1.1);
                background: var(--primary-hover);
            }

            @keyframes fadeInUp {
                from { opacity: 0; transform: translateY(30px); }
                to { opacity: 1; transform: translateY(0); }
            }

            /* Hover no Dropdown (Desktop) */
            @media (min-width: 992px) {
                .nav-item.dropdown:hover .dropdown-menu {
                    display: block;
                    animation: fadeInUp 0.3s ease;
                }
            }
        </style>
    </head>
    <body>

    <nav class="navbar navbar-expand-lg sticky-top navbar-custom">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fa-solid fa-scale-balanced me-2"></i>Sec. Mulher PE
            </a>
            
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <i class="fa-solid fa-bars text-primary"></i>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            Contratos
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ url_for('contrato.contratos_page') }}">Cadastrar Contrato</a></li>
                            <li><a class="dropdown-item" href="{{ url_for('contrato_produto.contrato_produto_page') }}">Estoque Contrato</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ url_for('contrato_produto.visualizar_contrato_produto_page') }}">Visualizar Estoques</a></li>
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            Notas Fiscais
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ url_for('nota_fiscal.cadastro_nota_fiscal_page') }}">Lançar Nota</a></li>
                            <li><a class="dropdown-item" href="{{ url_for('nota_fiscal.visualizar_notas_fiscais_page') }}">Histórico</a></li>
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            Gestão
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ url_for('controle_gasto.controle_gasto_page') }}">Controle de Gastos</a></li>
                            <li><a class="dropdown-item" href="{{ url_for('Relatorio.visualizar_relatorio_page') }}">Relatório Geral</a></li>
                        </ul>
                    </li>
                </ul>

                <div class="d-flex align-items-center">
                    <a href="{{ url_for('Notificacoes.notificaoes_page') }}" class="nav-link px-3">
                        <i class="fa-solid fa-bell"></i>
                    </a>
                    
                    <button class="theme-switch ms-2" id="themeBtn">
                        <i class="fa-solid fa-moon"></i>
                    </button>

                    <div class="dropdown ms-3">
                        <a class="nav-link p-0" href="#" data-bs-toggle="dropdown">
                            <i class="fa-solid fa-circle-user fa-2xl" style="color: var(--primary-color)"></i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li class="px-3 py-2"><small class="text-muted">Sessão ativa</small></li>
                            <li><a class="dropdown-item" href="{{ url_for('auth.login_page') }}">Sair / Trocar Conta</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <main class="main-hero">
        <div class="container text-center">
            <h1 class="hero-title">Secretaria da Mulher</h1>
            <p class="hero-subtitle mx-auto">
                Sistema Integrado de Gestão de Contratos e Execução Orçamentária.
                Transparência e eficiência no controle de recursos públicos.
            </p>
            <div class="mt-5 animate-up" style="animation-delay: 0.5s">
                <a href="{{ url_for('contrato.contratos_page') }}" class="btn btn-primary-custom px-5 py-3 rounded-pill shadow">
                    Começar Gestão <i class="fa-solid fa-arrow-right ms-2"></i>
                </a>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Gerenciador de Tema
        const themeBtn = document.getElementById('themeBtn');
        const htmlEl = document.documentElement;
        const themeIcon = themeBtn.querySelector('i');

        const toggleTheme = () => {
            const currentTheme = htmlEl.getAttribute('data-theme');
            const newTheme = currentTheme === 'light' ? 'dark' : 'light';
            
            htmlEl.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            
            themeIcon.className = newTheme === 'light' ? 'fa-solid fa-moon' : 'fa-solid fa-sun';
        };

        // Inicialização
        const savedTheme = localStorage.getItem('theme') || 'light';
        htmlEl.setAttribute('data-theme', savedTheme);
        themeIcon.className = savedTheme === 'light' ? 'fa-solid fa-moon' : 'fa-solid fa-sun';

        themeBtn.addEventListener('click', toggleTheme);
    </script>

    </body>
    </html>